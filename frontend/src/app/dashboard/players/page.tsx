import { ModulePlaceholder } from "@/components/dashboard/module-placeholder";

export default function PlayersPage() {
  return (
    <ModulePlaceholder
      title="Players"
      description="Registry of all federation players, with search, filtering, and CSV/Excel import."
      phase="Phase 2"
    />
  );
}
