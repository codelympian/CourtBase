import { ModulePlaceholder } from "@/components/dashboard/module-placeholder";

export default function RankingsPage() {
  return (
    <ModulePlaceholder
      title="Rankings"
      description="Configurable ranking engine: automatic points, current rankings, and history."
      phase="Phase 4"
    />
  );
}
